<StatusView>:
	BoxLayout:
		orientation: 'vertical'

		Label:
			id: vehicle_speed_label
			text: f'Vehicle speed: {root.current_speed}'
		Label:
			id: accelerator_position_label
			text: f'Relative accelerator position: {root.accelerator_position}'
